#reàd two input 
read -p "Enter first number: " a #input a
read -p "Enter second number: " b #input b
#Euclidean  algorithm 
while [ $b -ne 0 ]
do
    temp=$b
    b=$(( a % b ))
    a=$temp
done

echo "The Greatest Common Divisor (GCD) is: $a"
